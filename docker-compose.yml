services:
  web:
    image: ghcr.io/tpoderoso/tools.poderoso.io:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3004:3000"
    env_file:
      - .env
    volumes:
      - visits-data:/app/data
    restart: unless-stopped

volumes:
  visits-data:
